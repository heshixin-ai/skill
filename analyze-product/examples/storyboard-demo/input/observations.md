# 输入材料：模拟界面记录

本文件全部为虚构教学材料。顺序编号代替真实时间；引号内是构造的页面文案。没有真实截图、账号、网络请求、产品文档或后台日志。记录没有覆盖的行为应视为未知。

## E001 · 创建需求

- 用户在项目 `DEMO-P01` 输入：“为保温杯做一支 15 秒短片的三张分镜图。三个镜头各对应 5 秒，第一镜头是白天公园。”
- 配置栏显示：“3 个镜头”“每镜头 5 秒”“横屏 16:9”。
- 模型选择栏显示：“默认图像模型”，没有供应商或模型版本。
- 界面输出类型为“分镜图片”，没有视频文件。

## E002 · 脚本出现

- 聊天发送方：“脚本助手”。文案：“脚本已准备好，请确认后生成分镜。”
- 画布新增 `SCRIPT-01 / v1`，三行分别是“白天公园展示杯子”“倒入热水”“杯身特写”。
- 场景卡 `SCENE-01 / v1` 显示“白天公园”。
- 画布按钮：“确认脚本”。当前未显示图像资产。

## E003 · 用户确认

- 用户点击“确认脚本”。
- 画布状态：“脚本 v1 已确认”。
- 聊天发送方：“分镜助手”。文案：“开始生成三个镜头。”

## E004 · 任务运行

- 任务面板出现 `JOB-01`，状态“进行中”，子项 `SHOT-01`、`SHOT-02`、`SHOT-03`。
- 三张镜头卡显示“生成中”。
- 当前没有能打开的图像预览。

## E005 · 状态冲突

- 聊天发送方：“分镜助手”。文案：“三个分镜已全部生成完成。”
- 同一时点任务面板：`JOB-01` 状态“部分失败”；`SHOT-02` 显示“生成超时”。
- 资产栏：`IMG-01 / v1`、`IMG-03 / v1` 存在，打开两者预览成功。
- 第二镜头卡显示“无可用图片”，按钮为“重试此镜头”。

## E006 · 用户局部重试

- 用户点击第二镜头的“重试此镜头”。
- 任务面板新增 `JOB-02`，子项仅有 `SHOT-02`，状态“进行中”。
- 第一、第三镜头仍显示 `IMG-01 / v1`、`IMG-03 / v1`，没有出现新版本。
- 无费用记录或后端调用日志。

## E007 · 重试后可预览

- `JOB-02` 状态“完成”；历史任务 `JOB-01` 仍为“部分失败”。
- 第二镜头新增 `IMG-02 / v1`，预览能打开。
- 三镜头汇总状态：“可预览 3/3”。
- 第一镜头图片显示白天公园；其来源标签为 `SCENE-01 / v1`。
- 没有人工质量确认记录，也没有导出视频的记录。

## E008 · 用户修改场景

- 用户将第一镜头场景改为“夜晚公园”，点击“保存场景”。
- 场景卡变为 `SCENE-01 / v2`，内容“夜晚公园”。
- 页面未要求重新确认脚本。

## E009 · 下游仍为旧图

- 第一镜头图片仍为 `IMG-01 / v1`，预览仍是白天公园。
- 图片来源标签仍为 `SCENE-01 / v1`。
- 第一镜头卡状态：“最新”；项目汇总状态仍为“可预览 3/3”。
- 任务列表没有新增任务，也没有可见的“待更新”标记。

## E010 · 用户停止

- 用户发送：“先停在这里，不要继续生成。”
- 聊天发送方：“分镜助手”。文案：“已停止，保留当前结果。”
- 最后页面仍显示夜晚场景卡与白天旧图；没有新增任务。
- 观察在此结束，没有后续截图或可用日志。
